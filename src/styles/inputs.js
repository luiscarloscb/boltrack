import { StyleSheet } from "react-native";
import { whiteTrans } from "../utils/colors";

export const inputStyles = StyleSheet.create({
  authInput: {
    backgroundColor: whiteTrans,
    margin: 10,
    width: 300
  }
});
